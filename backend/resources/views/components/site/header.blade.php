<header class="sticky top-0 z-40 border-b border-line bg-paper/85 backdrop-blur">
    <div class="mx-auto flex h-16 max-w-6xl items-center justify-between px-5">
        <a href="{{ route('site.home') }}" class="text-xl font-black">زينو</a>

        <nav class="hidden items-center gap-7 text-sm font-bold text-muted md:flex">
            <a href="{{ route('site.home') }}" class="hover:text-ink">الرئيسية</a>
        </nav>

        <a href="#" class="rounded-control bg-amber px-5 py-2.5 text-sm font-extrabold text-ink">
            حمّل التطبيق
        </a>
    </div>
</header>
